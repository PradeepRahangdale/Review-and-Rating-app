# Local dev: leave empty (Vite proxy handles /api).
# Production default: https://review-and-rating-app.onrender.com (see .env.production)
# Override on Vercel only if your Render URL is different:
# VITE_API_URL=https://your-backend.onrender.com
# BACKEND_URL=https://your-backend.onrender.com
