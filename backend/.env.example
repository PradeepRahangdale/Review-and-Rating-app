PORT=5000
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/review-rating?retryWrites=true&w=majority
# Optional: direct URI if srv fails locally (Atlas → Connect → Drivers)
# MONGODB_URI_DIRECT=mongodb://<username>:<password>@<shard-host>:27017/review-rating?ssl=true&authSource=admin&retryWrites=true&w=majority
# Production: your Vercel/Netlify frontend URL (for CORS)
# FRONTEND_URL=https://your-app.vercel.app
